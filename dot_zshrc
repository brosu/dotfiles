# Download Znap, if it's not there yet.
[[ -f ~/Git/zsh-snap/znap.zsh ]] ||
    git clone --depth 1 -- \
        https://github.com/marlonrichert/zsh-snap.git ~/Git/zsh-snap

source ~/Git/zsh-snap/znap.zsh
znap prompt sindresorhus/pure

# znap source jeffreytse/zsh-vi-mode
znap source marlonrichert/zsh-autocomplete
# znap source zsh-users/zsh-autosuggestions
znap source zsh-users/zsh-syntax-highlighting
znap source se-jaeger/zsh-activate-py-environment
znap source gretzky/auto-color-ls
znap source Kallahan23/zsh-colorls
znap source Tarrasch/zsh-bd
znap source arzzen/calc.plugin.zsh
znap source zpm-zsh/clipboard
znap source zpm-zsh/colorize
#znap source RobSis/zsh-completion-generator # needs python
znap source ChrisPenner/copy-pasta
znap source z-shell/zsh-diff-so-fancy
#znap source Senderman/doas-zsh-plugin # prefix your current or previous commands with doas by pressing ESC twice.
znap source hcgraf/zsh-sudo # prefix your current or previous commands with sudo by pressing ESC twice.
znap source psprint/zsh-editing-workbench # Incremental history word completing
znap source b4b4r07/enhancd
# znap source hkupty/ssh-agent 
# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:/usr/local/bin:$PATH

export LOCAL_IP=$(ifconfig | grep "inet " | grep -v "127" | cut -d ' ' -f 2)
source ~/.zsh/settings.zsh
source ~/.zsh/aliases.zsh
eval "$(logcli --completion-script-zsh)"


# Load Angular CLI autocompletion.
source <(ng completion script)
source <(npm completion)
source <(kubefwd completion zsh)
compdef _kubefwd kubefwd
eval export PATH="/Users/bogdan.rosu/.jenv/shims:${PATH}"
export JENV_SHELL=zsh
export JENV_LOADED=1
unset JAVA_HOME
unset JDK_HOME
source '/opt/homebrew/Cellar/jenv/0.5.6/libexec/libexec/../completions/jenv.zsh'
jenv rehash 2>/dev/null
jenv refresh-plugins
jenv() {
  type typeset &> /dev/null && typeset command
  command="$1"
  if [ "$#" -gt 0 ]; then
    shift
  fi

  case "$command" in
  enable-plugin|rehash|shell|shell-options)
    eval `jenv "sh-$command" "$@"`;;
  *)
    command jenv "$command" "$@";;
  esac
}
export PYENV_ROOT="$HOME/.pyenv"
command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"
